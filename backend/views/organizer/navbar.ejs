<!-- Global Page Preloader -->
<div id="global-preloader">
    <img src="/public/NEWLOGO.webp" alt="Loading..." class="preloader-logo">
</div>
<style>
    #global-preloader {
        position: fixed;
        top: 0; left: 0; width: 100%; height: 100%;
        background: #08080a;
        z-index: 999999;
        display: flex;
        justify-content: center;
        align-items: center;
        transition: opacity 0.4s ease, visibility 0.4s ease;
    }
    .preloader-logo {
        width: 80px;
        height: auto;
        animation: pulse-preloader 1.2s infinite alternate cubic-bezier(0.4, 0, 0.2, 1);
    }
    @keyframes pulse-preloader {
        0% { transform: scale(0.9); opacity: 0.6; filter: drop-shadow(0 0 5px rgba(255,87,34,0.3)); }
        100% { transform: scale(1.1); opacity: 1; filter: drop-shadow(0 0 20px rgba(255,87,34,0.8)); }
    }
    body.loaded #global-preloader {
        opacity: 0;
        visibility: hidden;
    }
</style>
<script>
    // Fades out the preloader once the entire page (including images) is fully loaded
    window.addEventListener('load', () => {
        document.body.classList.add('loaded');
    });
    // Fallback: If page takes longer than 3 seconds to load, remove preloader anyway
    setTimeout(() => { document.body.classList.add('loaded'); }, 3000);
</script>

<!-- High-end Smooth Scrolling & Page Navigation Transitions -->
<script>
    document.addEventListener('DOMContentLoaded', () => {
        // Intercept internal links to instantly show the logo preloader for seamless travelling feel
        document.querySelectorAll('a').forEach(link => {
            link.addEventListener('click', function(e) {
                if (this.target === '_blank' || 
                    this.getAttribute('href')?.startsWith('#') || 
                    this.hasAttribute('onclick') || 
                    e.ctrlKey || e.metaKey || 
                    !this.href) return;
                
                try {
                    const currentUrl = new URL(window.location.href);
                    const targetUrl = new URL(this.href);
                    
                    if (currentUrl.origin === targetUrl.origin && currentUrl.pathname !== targetUrl.pathname) {
                        e.preventDefault();
                        // Instantly show the preloader by removing 'loaded' class
                        document.body.classList.remove('loaded');
                        // Allow small time for preloader to fade in, then navigate
                        setTimeout(() => { window.location.href = this.href; }, 150);
                    }
                } catch(err) {}
            });
        });
    });

    // Ensure preloader is hidden if user navigates back via browser history (bfcache)
    window.addEventListener('pageshow', (event) => {
        if (event.persisted) {
            document.body.classList.add('loaded');
        }
    });
</script>

<nav class="nav scrolled" id="nav">
    <div class="nav-container">
        <a href="/organizer" class="logo" id="logo">
            <img src="/public/NEWLOGO.webp" alt="Blaze Frontier Logo" class="logo-img">
            <span class="logo-name">BLAZE<span class="logo-highlight">FRONTIER</span></span>
        </a>
        <div style="display: flex; align-items: center; gap: 20px;">
            <div class="nav-menu" id="nav-menu">
                <div class="nav-item" style="padding: 8px 12px; background: rgba(0,0,0,0.5); border: 1px solid rgba(255,87,34,0.3); display: flex; align-items: center; gap: 8px; cursor: default; justify-content: space-between;">
                    <input type="text" id="nav-search-blaze-id" placeholder="Search Blaze ID" onkeypress="if(event.key === 'Enter'){ event.preventDefault(); searchUserProfileNav(); }" style="background: transparent; border: none; color: #fff; outline: none; width: 100%; font-size: 0.9rem;">
                    <button type="button" onclick="searchUserProfileNav()" style="background: transparent; border: none; color: var(--blaze-orange); cursor: pointer; padding: 4px; display: flex; align-items: center; justify-content: center;"><svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg></button>
                </div>
                <a href="/organizer" class="nav-item <%= typeof activePage !== 'undefined' && activePage === '/organizer' ? 'active' : '' %>">
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="7" height="9"/><rect x="14" y="3" width="7" height="5"/><rect x="14" y="12" width="7" height="9"/><rect x="3" y="16" width="7" height="5"/></svg>
                    <span>Dashboard</span>
                </a>
                <a href="/organizer/tournaments" class="nav-item <%= typeof activePage !== 'undefined' && activePage === '/organizer/tournaments' ? 'active' : '' %>">
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg>
                    <span>Tournaments</span>
                </a>
                <a href="/organizer/results" class="nav-item <%= typeof activePage !== 'undefined' && activePage === '/organizer/results' ? 'active' : '' %>">
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/></svg>
                    <span>Results</span>
                </a>
                <a href="/organizer/registrations" class="nav-item <%= typeof activePage !== 'undefined' && activePage === '/organizer/registrations' ? 'active' : '' %>">
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M16 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="8.5" cy="7" r="4"/><line x1="20" y1="8" x2="20" y2="14"/><line x1="23" y1="11" x2="17" y2="11"/></svg>
                    <span>Registrations</span>
                </a>
                <a href="/organizer/challenges" class="nav-item <%= typeof activePage !== 'undefined' && activePage === '/organizer/challenges' ? 'active' : '' %>" style="color: #a855f7; text-shadow: 0 0 8px rgba(168,85,247,0.4);">
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M14.5 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7.5L14.5 2z"/><polyline points="14 2 14 8 20 8"/><path d="M12 18v-6"/><path d="M8 15h8"/></svg>
                    <span>Challenges</span>
                </a>
                <a href="/organizer/clips" class="nav-item <%= typeof activePage !== 'undefined' && activePage === '/organizer/clips' ? 'active' : '' %>">
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="2" width="20" height="20" rx="2.18" ry="2.18"/><line x1="7" y1="2" x2="7" y2="22"/><line x1="17" y1="2" x2="17" y2="22"/><line x1="2" y1="12" x2="22" y2="12"/><line x1="2" y1="7" x2="7" y2="7"/><line x1="2" y1="17" x2="7" y2="17"/><line x1="17" y1="17" x2="22" y2="17"/><line x1="17" y1="7" x2="22" y2="7"/></svg>
                    <span>Clips</span>
                </a>
                <a href="/organizer/daily" class="nav-item <%= typeof activePage !== 'undefined' && activePage === '/organizer/daily' ? 'active' : '' %>">
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/></svg>
                    <span>Highlights</span>
                </a>
                <a href="#" class="nav-item" id="org-logout-btn" style="color: var(--text-muted);" onclick="event.preventDefault(); const cacheKey = window.getBlazeCacheKey ? window.getBlazeCacheKey() : 'blaze_profile_cache'; localStorage.removeItem(cacheKey); localStorage.removeItem('blaze_token'); localStorage.removeItem('blaze_role'); window.location.href='/auth';">
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/><polyline points="16 17 21 12 16 7"/><line x1="21" y1="12" x2="9" y2="12"/></svg>
                    <span>Logout</span>
                </a>
            </div>

            <div class="mobile-toggle" id="mobile-toggle" style="cursor: pointer;">
                <span></span>
                <span></span>
                <span></span>
            </div>
        </div>
    </div>
</nav>

<style>
    @media (max-width: 900px) {
        .mobile-toggle {
            display: flex !important;
            z-index: 1001;
        }
        .mobile-toggle.active span:nth-child(1) { transform: translateY(6.5px) rotate(45deg); }
        .mobile-toggle.active span:nth-child(2) { opacity: 0; }
        .mobile-toggle.active span:nth-child(3) { transform: translateY(-6.5px) rotate(-45deg); }

        .nav-menu {
            position: fixed;
            top: 72px;
            left: 0;
            width: 100%;
            height: calc(100vh - 72px);
            background: rgba(8, 8, 12, 0.98);
            border-bottom: 2px solid rgba(255, 87, 34, 0.3);
            flex-direction: column;
            padding: 40px 20px;
            transform: translateY(-100%);
            opacity: 0;
            visibility: hidden;
            transition: transform 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.05), opacity 0.3s ease, visibility 0.4s;
            display: flex !important;
            z-index: 998;
            align-items: center;
            gap: 15px;
            overflow-y: auto;
        }
        .nav-menu.active {
            transform: translateY(0);
            opacity: 1;
            visibility: visible;
        }
        
        .nav-menu::before {
            content: '';
            position: absolute;
            top: 0; left: 0; right: 0; bottom: 0;
            background: 
                /* Subtle ambient top lighting using brand orange */
                radial-gradient(ellipse at 50% -20%, rgba(255, 87, 34, 0.12) 0%, transparent 60%),
                /* Deep dark obsidian base fade */
                linear-gradient(180deg, #0a0a0c 0%, #050506 100%),
                /* Faint premium dot texture */
                radial-gradient(rgba(255,255,255,0.025) 1px, transparent 1px);
            background-size: 100% 100%, 100% 100%, 20px 20px;
            box-shadow: inset 0 1px 0 rgba(255,255,255,0.04);
            opacity: 0;
            pointer-events: none;
            transition: opacity 0.5s ease;
            z-index: -1;
        }
        
        .nav-menu.active::before {
            opacity: 1;
            transition-delay: 0.2s;
        }
        
        .nav-item {
            width: 100%;
            max-width: 320px;
            padding: 16px 24px;
            border-radius: 12px;
            background: rgba(255, 255, 255, 0.02);
            border: 1px solid rgba(255,255,255,0.04);
            text-align: left;
            font-size: 1.1rem;
            letter-spacing: 1px;
            font-weight: 600;
            opacity: 0;
            transform: translateX(-20px);
            transition: all 0.25s cubic-bezier(0.16, 1, 0.3, 1);
            position: relative;
            overflow: hidden;
            display: flex;
            align-items: center;
            color: var(--text-300);
        }
        .nav-menu.active .nav-item {
            opacity: 1;
            transform: translateX(0);
        }
        
        /* Staggered animation */
        .nav-menu.active .nav-item:nth-child(1) { transition-delay: 0.1s; }
        .nav-menu.active .nav-item:nth-child(2) { transition-delay: 0.14s; }
        .nav-menu.active .nav-item:nth-child(3) { transition-delay: 0.18s; }
        .nav-menu.active .nav-item:nth-child(4) { transition-delay: 0.22s; }
        .nav-menu.active .nav-item:nth-child(5) { transition-delay: 0.26s; }
        .nav-menu.active .nav-item:nth-child(6) { transition-delay: 0.30s; }
        
        .nav-item:active, .nav-item.active {
            background: rgba(255, 87, 34, 0.08);
            border-color: rgba(255, 87, 34, 0.25);
            color: #ff5722;
            transform: scale(0.98);
            box-shadow: 0 4px 15px rgba(255, 87, 34, 0.05);
        }
        
        .nav-item::before, .nav-item::after { display: none; }
    }
</style>

<script>
    // Session / Idle Tracking (30 mins inactivity = signout)
    (function() {
        const token = localStorage.getItem('blaze_token');
        if (!token) return;

        const IDLE_TIMEOUT_MS = 30 * 60 * 1000;
        const checkIdle = () => {
            const lastActive = localStorage.getItem('blaze_last_active');
            if (lastActive && (Date.now() - parseInt(lastActive) > IDLE_TIMEOUT_MS)) {
                localStorage.removeItem('blaze_token');
                localStorage.removeItem('blaze_last_active');
                localStorage.removeItem('blaze_role');
                window.location.href = '/auth';
            }
        };

        const updateActivity = () => {
            localStorage.setItem('blaze_last_active', Date.now().toString());
        };

        checkIdle();
        updateActivity();

        ['mousemove', 'keydown', 'click', 'scroll'].forEach(evt => {
            document.addEventListener(evt, () => {
                const now = Date.now();
                const last = parseInt(localStorage.getItem('blaze_last_active') || '0');
                if (now - last > 5000) {
                    updateActivity();
                }
            }, { passive: true });
        });

        setInterval(checkIdle, 60000);
    })();

    const mobileToggle = document.getElementById('mobile-toggle');
    const navMenu = document.getElementById('nav-menu');

    if (mobileToggle && navMenu) {
        mobileToggle.addEventListener('click', (e) => {
            e.stopImmediatePropagation();
            mobileToggle.classList.toggle('active');
            navMenu.classList.toggle('active');
        });
    }
</script>

<!-- View Profile Modal (Global) -->
<div class="org-modal-overlay" id="viewProfileModal">
    <div class="org-modal" style="max-width: 450px; padding: 24px;">
        <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 24px;">
            <h2 style="margin: 0; font-size: 1.5rem; text-transform: uppercase;">Player Profile</h2>
            <button onclick="closeViewProfileModal()" style="background: transparent; border: none; color: var(--text-muted); cursor: pointer; font-size: 1.5rem;">&times;</button>
        </div>
        
        <div id="search-loading" style="display: none; text-align: center; padding: 20px; color: var(--text-muted);">
            Searching database...
        </div>
        
        <div id="search-error" style="display: none; text-align: center; padding: 40px 20px; color: #ef4444; font-family: var(--font-display); font-size: 1.2rem; letter-spacing: 1px;">
            <div style="font-size: 3rem; margin-bottom: 10px;">??</div>
            NO PROFILE EXISTS
        </div>

        <div id="search-result" style="display: none;">
            <div style="display: flex; align-items: center; gap: 16px; margin-bottom: 24px; padding-bottom: 20px; border-bottom: 1px solid rgba(255,255,255,0.06);">
                <img id="sp-avatar" src="/public/blaze_avatar.png" style="width: 80px; height: 80px; border-radius: 50%; object-fit: cover; border: 2px solid rgba(255,255,255,0.1);">
                <div>
                    <div id="sp-name" style="font-family: var(--font-display); font-size: 1.8rem; color: #fff; line-height: 1;">—</div>
                    <div id="sp-badge" style="font-size: 0.75rem; font-family: var(--font-mono); margin-top: 6px; padding: 2px 6px; border-radius: 4px; display: inline-block;"></div>
                </div>
            </div>
            
            <div style="display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 12px; margin-bottom: 20px;">
                <div style="background: rgba(0,0,0,0.2); padding: 12px; border-radius: 8px; border: 1px solid rgba(255,215,0,0.2);">
                    <div style="font-size: 0.65rem; color: #fcd34d; text-transform: uppercase; font-weight: bold;">Blaze Coins</div>
                    <div id="sp-bc" style="font-family: var(--font-display); font-size: 1.5rem; color: #fff;">0</div>
                </div>
                <div style="background: rgba(0,0,0,0.2); padding: 12px; border-radius: 8px; border: 1px solid rgba(255,255,255,0.03);">
                    <div style="font-size: 0.65rem; color: var(--text-muted); text-transform: uppercase; font-weight: bold;">Matches</div>
                    <div id="sp-matches" style="font-family: var(--font-display); font-size: 1.5rem; color: #fff;">0</div>
                </div>
                <div style="background: rgba(0,0,0,0.2); padding: 12px; border-radius: 8px; border: 1px solid rgba(255,255,255,0.03);">
                    <div style="font-size: 0.65rem; color: var(--text-muted); text-transform: uppercase; font-weight: bold;">Wins</div>
                    <div id="sp-wins" style="font-family: var(--font-display); font-size: 1.5rem; color: #fff;">0</div>
                </div>
            </div>
            
            <div style="display: flex; flex-direction: column; gap: 10px; background: rgba(255,255,255,0.02); padding: 16px; border-radius: 8px; border: 1px solid rgba(255,255,255,0.05);">
                <div style="display: flex; justify-content: space-between; font-size: 0.9rem;">
                    <span style="color: var(--text-muted);">Username</span>
                    <span id="sp-user" style="color: #fff; font-family: var(--font-mono);">—</span>
                </div>
                <div style="display: flex; justify-content: space-between; font-size: 0.9rem;">
                    <span style="color: var(--text-muted);">Blaze ID</span>
                    <span id="sp-pid" style="color: var(--blaze-orange); font-family: var(--font-mono); font-weight: bold;">—</span>
                </div>
                <div style="display: flex; justify-content: space-between; font-size: 0.9rem;">
                    <span style="color: var(--text-muted);">Game UID</span>
                    <span id="sp-uid" style="color: #fff; font-family: var(--font-mono);">—</span>
                </div>
                <div style="display: flex; justify-content: space-between; font-size: 0.9rem;">
                    <span style="color: var(--text-muted);">Region</span>
                    <span id="sp-region" style="color: #fff; font-family: var(--font-mono);">—</span>
                </div>
                <div style="display: flex; justify-content: space-between; font-size: 0.9rem;">
                    <span style="color: var(--text-muted);">Setup Complete</span>
                    <span id="sp-setup" style="color: #22c55e; font-family: var(--font-mono); font-weight: bold;">—</span>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    document.addEventListener('DOMContentLoaded', () => {
        const modal = document.getElementById('viewProfileModal');
        if(modal) {
            modal.addEventListener('click', (e) => {
                if (e.target === e.currentTarget) closeViewProfileModal();
            });
        }
    });

    function closeViewProfileModal() {
        const modal = document.getElementById('viewProfileModal');
        if(modal) modal.classList.remove('active');
    }

    async function searchUserProfileNav() {
        const pid = document.getElementById('nav-search-blaze-id').value.trim();
        if (!pid) {
            if(typeof showToast === 'function') {
                showToast('Please enter a Blaze ID', 'error');
            } else {
                alert('Please enter a Blaze ID');
            }
            return;
        }
        
        const modal = document.getElementById('viewProfileModal');
        modal.classList.add('active');
        
        document.getElementById('search-loading').style.display = 'block';
        document.getElementById('search-result').style.display = 'none';
        document.getElementById('search-error').style.display = 'none';
        
        try {
            const token = localStorage.getItem('blaze_token');
            const res = await fetch('/api/organizer/player/' + pid, {
                headers: { 'x-auth-token': token }
            });
            
            if (!res.ok) {
                document.getElementById('search-loading').style.display = 'none';
                document.getElementById('search-error').style.display = 'block';
                return;
            }
            
            const data = await res.json();
            document.getElementById('sp-name').textContent = data.inGameName || data.username;
            document.getElementById('sp-avatar').src = data.profilePic || '/public/blaze_avatar.png';
            document.getElementById('sp-bc').textContent = data.blazeCoins || 0;
            document.getElementById('sp-matches').textContent = data.totalMatches || 0;
            document.getElementById('sp-wins').textContent = data.tournamentsWon || 0;
            document.getElementById('sp-user').textContent = data.username || '—';
            document.getElementById('sp-pid').textContent = data.playerId;
            document.getElementById('sp-uid').textContent = data.gameUid || '—';
            document.getElementById('sp-region').textContent = data.location || '—';
            
            const setupEl = document.getElementById('sp-setup');
            if (data.isSetupComplete) {
                setupEl.textContent = 'COMPLETE';
                setupEl.style.color = '#22c55e';
            } else {
                setupEl.textContent = 'PENDING';
                setupEl.style.color = '#ef4444';
            }
            
            const badge = document.getElementById('sp-badge');
            if (data.role === 'organizer') {
                badge.textContent = 'ORGANIZER';
                badge.style.background = 'rgba(139, 92, 246, 0.2)';
                badge.style.color = '#a78bfa';
                badge.style.border = '1px solid rgba(139, 92, 246, 0.4)';
            } else if (data.isGenuine) {
                badge.textContent = 'VERIFIED PLAYER';
                badge.style.background = 'rgba(14, 165, 233, 0.2)';
                badge.style.color = '#38bdf8';
                badge.style.border = '1px solid rgba(14, 165, 233, 0.4)';
            } else {
                badge.textContent = 'NEW PLAYER';
                badge.style.background = 'rgba(255, 255, 255, 0.05)';
                badge.style.color = 'var(--text-muted)';
                badge.style.border = '1px solid rgba(255, 255, 255, 0.1)';
            }
            
            document.getElementById('search-loading').style.display = 'none';
            document.getElementById('search-result').style.display = 'block';
        } catch (err) {
            console.error('Search error:', err);
            document.getElementById('search-loading').style.display = 'none';
            document.getElementById('search-error').style.display = 'block';
        }
    }
</script>




